
// {
// const processInput = (value: string | number): string | number | undefined => {
//     if (typeof value === 'string') {
//         return value.length;
//     } else if (typeof value === 'number') {
//         return value * value;
//     } else {
//         // Handle other types or throw an error if needed.
//         console.log('please input string or number');
//     }
// }

// // Example usage:
// const strLength = processInput("Hello, World"); // Returns the length of the string (12)
// const squaredNum = processInput(5); // Returns the square of the number (25)
// }

// function processUnionType(input: string | number): number {
//     if (typeof input === "string") {
//         // If it's a string, return its length
//         return input.length;
//     } else if (typeof input === "number") {
//         // If it's a number, return the square of that number
//         return input * input;
//     } else {
//         // Handle other types or unexpected input
//         throw new Error("Input must be a string or a number");
//     }
// }

// // Example usage:
// const stringResult = processUnionType("Hello amit");
// const numberResult = processUnionType(5);

// console.log("String Result:", stringResult); // Logs the length of the string, i.e., 5
// console.log("Number Result:", numberResult); // Logs the square of the number, i.e., 9



